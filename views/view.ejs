<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        p{
            font-family:'Times New Roman', Times, serif;
            font-size: 1.5vw;
        }

        h1 {
            font-family:'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
            font-size: 5vw;
            color: rgba(209, 0, 164, .5);
            margin-left: 13vw;
        }

        img{
            width: 40vw;
            border-radius: 600px;
            float: left;
            margin-right: 10px;
        }

        .box {
            display:inline;
        }

        .userName {
            font-weight: bold;
        }

        .nav-item {
            display: inline-flex;
        }

        .nav-link{
            position: relative;
            left: 0;
            font-size: 2rem;
        }

        .nav-left {            
            font-size: 2rem;
            position: absolute;
            right: 0;
            margin: 2vw 2vh 0 0;
            border: none;
            background-color: white;
            font-family: 'Times New Roman', Times, serif;
            color: rgba(217, 2, 45, .5);
        }
    </style>
</head>
<body>

    <div class="nav-item">
        <a class="nav-link" href="/">Admin Login</a>
        <% if (isAdminRedirect==='yes' ){ %>
        <form action="/edit/<%= userData._id %>" method="get">
            <button type="submit" class="nav-left"> Edit </button>
        </form>
        <% } %>
    </div>

    <div>
        <h1> Hi <%= userData.firstName %> </h1>
    </div>
    <div class="box">
        <div id="image">
        <img src=<%= url+userData.photo %> alt=<%= userData.name %> loading="eager">
        </div>
        <div>
            <!-- <p> <%= userData.photo %> </p> -->
            <p> <%= userData.name %>  </p>
            <p> <%= userData.email %>  </p>
            <p> <%= userData.number %>  </p>
            <p> <%= userData.job %>  </p>
            <p> <%= (userData.married == true) ? "Married" : "Not Married" %> </p>
            <p> <%= userData.people %> people stay </p>
            <p> <%= userData.vichele %> vichele </p>
        </div>
    </div>
</body>
</html>